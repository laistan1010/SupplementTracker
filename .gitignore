# Secrets — the Grok/xAI key lives in Vercel env vars, NEVER committed
.env
.env.*
*.local

# Deps / build
node_modules/
.vercel/

# OS / editor cruft
.DS_Store
Thumbs.db
.gstack/

# golden-test photos stay local (personal photos, keep repo lean)
tests/golden/photos/*
!tests/golden/photos/.gitkeep
